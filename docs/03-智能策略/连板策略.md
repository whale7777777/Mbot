                                                                                                                                                                          # 连板策略

基于涨停池数据的**连板晋级概率**统计、滚动回测校准与每日选股参考。适用于 A 股短线情绪周期中，对「昨日连板股今日能否再多一封」进行量化筛选（非自动下单，需结合盘面与风控自行决策）。

---

## 1. 策略逻辑

### 1.1 核心概念

| 术语 | 定义 |
|------|------|
| **n 连板** | 当日收盘涨停池中，该股「连板数」字段为 n（首板 n=1） |
| **晋级** | T 日 n 连板的个股，T+1 日仍在涨停池且连板数为 **n+1** |
| **晋级概率** | 历史上同连板高度 n 的样本中，完成晋级的比例（经平滑与因子修正） |
| **连板股（本策略默认）** | 连板数 ≥ 2，剔除 ST、*ST 及名称含「退」的标的 |

媒体常说的「连板晋级率」一般指：**T 日连板数 ≥ 2** 的样本在 T+1 再多封一天的比例（不含首板）。

### 1.2 预测思路

1. 用 **T 日之前** 若干相邻交易日的涨停池，统计各连板高度 n → n+1 的实证晋级率。
2. 对样本少的档位做 **拉普拉斯平滑**，并向整体连板晋级率 **收缩**，避免极端估计。
3. 结合个股当日 **封板质量因子**（炸板次数、封板资金等）做乘子修正。
4. 用近段交易日做 **滚动回测**，按分层偏差微调各档概率，写入校准文件供次日使用。

### 1.3 因子说明（校准后）

| 因子键 | 含义 | 规则 |
|--------|------|------|
| `zero_zhaban` | 无炸板 | 当日炸板次数 = 0 |
| `heavy_seal` | 强封单 | **封板资金 / 当日成交额 ≥ 20%**（与该股成交匹配，非固定金额） |
| `early_seal` | 早盘封板 | 首次封板时间不晚于 10:30 |
| `big_order_seal` | 大单封板 | **封板资金 / 当日成交额 ≥ 12%** 且 < 20%（中档；≥20% 归入强封单） |
| `weak_to_strong` | 弱转强 | 炸板次数 ≥ 1，**或** 最后封板较首次封板晚 ≥ 5 分钟 |

每日列表会展示 **涨停原因**（同花顺涨停池 `reason_type`，可在配置中改为 `tushare`）、**封单占比**、**大单封板**、**弱转强**，并参与晋级概率乘子。

涨停原因配置（`lianban_config.json`）：

| 配置项 | 说明 |
|--------|------|
| `zt_reason_source` | `ths`（默认，同花顺）或 `tushare`（需 token，`limit_list_ths.lu_desc`） |
| `zt_reason_fallback` | 主源失败时是否回退到同花顺 |

因子以**相对基准晋级率的乘子**形式叠加（15 日实证校准，见 `连板预测校准.json`）。极高连板（样本极少）时，回退到不高于当前高度的最近一档历史率。

### 1.4 同线 / 类似标的

生成每日文档时会按 **涨停原因关键词 + 行业** 聚类，并标注：

| 字段 | 说明 |
|------|------|
| **题材簇** | 如「磷化铟/半导体材料」「光纤/四氯化硅」 |
| **类似标的** | 同簇其他个股，如 `同线: 三孚股份、宏柏新材` |
| **似近月龙头** | 与 `近月高标龙头分析.md` 同主线的历史高标 |

回答「值得买的票」或解读连板时，**必须列出同线类似票**及角色（空间龙 / 中军 / 补涨 / 弱标慎追）。逻辑见 `scripts/lianban_similar.py`。

---

## 2. 数据与配置

### 2.1 数据源

| 优先级 | 来源 | 接口 | 历史长度 |
|--------|------|------|----------|
| **默认** | 东方财富 | `akshare.stock_zt_pool_em` | 约 15 个交易日 |
| 可选 | TuShare Pro | `limit_list_d`（`limit_type='U'`） | 可拉 30+ 交易日（需 token） |

统一封装在 `scripts/lianban_data.py`，列名对齐东方财富口径，下游脚本无感切换。

### 2.2 配置文件

路径：`docs/03-智能策略/连板数据/lianban_config.json`（模板：`lianban_config.example.json`）

```json
{
  "data_source": "em",
  "tushare_token": "",
  "tushare_token_env": "TUSHARE_TOKEN",
  "read_token_from": "../../utils/configure/config.json",
  "read_token_field": "ts_token",
  "fallback_to_em": true,
  "trade_days_target": 35,
  "lookback_pairs": 20,
  "validate_pairs": 30,
  "min_boards": 2,
  "laplace_alpha": 1.0,
  "shrink_to_overall": 0.15
}
```

**Token 优先级（高 → 低）**

1. 环境变量 `LIANBAN_TUSHARE_TOKEN`
2. 环境变量 `TUSHARE_TOKEN`
3. `lianban_config.json` → `tushare_token`
4. `utils/configure/config.json` → `ts_token`

TuShare 涨跌停列表需 [Pro 积分](https://tushare.pro/document/2?doc_id=298)。

### 2.3 依赖安装

```bash
pip install tushare akshare pandas
```

验证数据源：

```bash
python scripts/lianban.py test
```

akshare 模式下有 1 个交易日以上即通过；TuShare 模式建议 ≥ 30 日。

---

## 3. 统一入口与产出

所有连板能力收敛到 **`scripts/lianban.py`**（本文档为唯一说明）。内部模块仍位于 `scripts/lianban_*.py`，一般无需直接调用。

### 3.1 命令一览

| 命令 | 作用 |
|------|------|
| `python scripts/lianban.py` | **默认**：生成**当日连板文档** + 更新数据总览 |
| `python scripts/lianban.py today` | 同上，可 `--date YYYYMMDD` |
| `python scripts/lianban.py all` | 回测校准 + 当日文档 + 总览 |
| `python scripts/lianban.py backtest` | 仅滚动回测与校准 JSON |
| `python scripts/lianban.py weekly` | 连板晋级率周跟踪 |
| `python scripts/lianban.py publish` | 仅刷新 `数据总览.md` |
| `python scripts/lianban.py test` | 检查 akshare / 配置 |
| `python scripts/lianban.py init` | 生成 `lianban_config.json` |
| `python scripts/lianban.py batch --days 15` | **批量**生成最近 15 个交易日 `每日/YYYYMMDD.*` |

### 3.2 日常使用（收盘后）

```bash
# 一条命令 → 输出当日连板 Markdown
python scripts/lianban.py
```

主输出目录 **`docs/03-智能策略/连板数据/每日/`**（每个交易日单独文件）：

- **`YYYYMMDD.md`** — 当日连板 Markdown
- **`YYYYMMDD.json`** — 当日结构化数据

示例：`每日/20260519.md`、`每日/20260519.json`

首次使用或换行情阶段后，建议先跑一次全量：

```bash
python scripts/lianban.py all
```

可选参数示例：

```bash
python scripts/lianban.py today --date 20260519
python scripts/lianban.py today --min-boards 1
python scripts/lianban.py all --lookback 10
```

### 3.3 内部模块（供维护）

| 模块 | 作用 |
|------|------|
| `lianban.py` | **统一入口** |
| `lianban_data.py` | akshare / TuShare 涨停池 |
| `lianban_lib.py` | 晋级概率与校准逻辑 |
| `lianban_today.py` | 生成当日分析 Markdown |
| `lianban_backtest_30d.py` | 滚动回测 |
| `lianban_jinji_weekly.py` | 周报 |
| `lianban_publish_docs.py` | 汇总 `数据总览.md` |
| `lianban_paths.py` | 输出路径常量 |

### 3.4 输出文件（文档目录）

| 路径 | 说明 |
|------|------|
| [`连板数据/每日/YYYYMMDD.md`](./连板数据/每日/) | **按日落盘**（文件名=交易日） |
| [`连板数据/每日/YYYYMMDD.json`](./连板数据/每日/) | 同日 JSON |
| [`连板数据/数据总览.md`](./连板数据/数据总览.md) | 索引 + 最新一日摘要 |
| `连板数据/连板预测校准.json` | 校准参数 |
| `连板数据/连板滚动回测.md` | 回测摘要 |
| `连板数据/连板滚动验证明细.md` | 逐股验证 |
| `连板数据/连板晋级率周跟踪.md` | 周报 |

---

## 4. 晋级概率计算公式（简述）

对连板数为 \(n\) 的个股：

1. **基准率** \(p_n\)：由 T 日前 `lookback_pairs` 对相邻日中，所有「T 日 n 连板 → T+1 日 n+1」的样本汇总，再经拉普拉斯平滑与向整体率收缩。
2. **因子修正**：\(p = \mathrm{clip}\big(p_n \times \prod_i m_i,\ 0.02,\ 0.92\big)\)，其中 \(m_i\) 为触发的因子乘子（如 `zero_zhaban`、`heavy_seal`）。
3. **分层微调**：滚动回测后对各档 \(n\) 做有限幅度（约 ±15%）修正，写入 `连板预测校准.json`。

**命中判定（回测）**：T 日收盘后给出预测；T+1 收盘后若该股仍在涨停池且连板数 = n+1，则记为晋级命中。

---

## 5. 回测指标说明

| 指标 | 含义 |
|------|------|
| **Brier 分数** | 概率预测均方误差，越低越好 |
| **阈值 0.5 命中率** | 以 50% 为界二分类的准确率（仅供参考） |
| **实证晋级率** | 验证样本中实际晋级的比例 |

回测采用**严格 walk-forward**：预测 T 日个股时，仅使用 T 日之前的交易对，不使用未来信息。

未配置 TuShare 时，历史仅约 15 日，脚本会自动降低 `lookback` 与验证对数，并在 stderr 提示配置 token 以跑满 30 日。

---

## 6. 使用建议与风险

1. **概率≠建议满仓**：晋级概率用于排序与过滤，需结合板块强度、龙头地位、监管与流动性。
2. **定期重校准**：情绪周期切换后应重跑 `lianban_backtest_30d.py`。
3. **数据延迟**：TuShare 日线涨停数据通常在收盘后更新；盘中请以实时行情为准。
4. **样本偏差**：极高连板（6 板以上）样本少，估计不稳定，宜降仓或观望。
5. **合规**：本工具仅供研究学习，不构成投资建议。

---

## 7. 目录结构（相关）

```
scripts/
  lianban.py               # ★ 统一入口（执行即出当日文档）
  lianban_*.py             # 内部模块

docs/03-智能策略/
  连板策略.md              # 本文档（脚本说明合集）
  连板数据/
    每日/                  # 按日：YYYYMMDD.md / .json
    数据总览.md
    连板滚动回测.md
    ...
```

---

## 8. 常见问题

**Q：晋级概率全是 0 或异常？**  
检查相邻交易日顺序是否正确；需先跑回测生成校准文件，并确认数据源能拉到 T 与 T+1 两日涨停池。

**Q：只有 15 个交易日？**  
当前回退到了东方财富接口。请配置 TuShare token 并将 `data_source` 设为 `tushare`。

**Q：与「涨停开板」策略的关系？**  
连板策略关注**连续封板能否延续**；开板策略关注涨停后打开再封等盘口行为，二者可并列使用、分别建模。

---

*文档随 `scripts/lianban_*.py` 维护；参数以 `lianban_config.json` 与 `连板预测校准.json` 为准。*
---

## 9. 数据文档（自动生成）

运行 `python scripts/lianban.py` 后：

| 位置 | 说明 |
|------|------|
| [连板数据/每日/](./连板数据/每日/) | 按日：`YYYYMMDD.md`、`YYYYMMDD.json` |
| [连板数据/数据总览.md](./连板数据/数据总览.md) | 索引与最新摘要 |
| [连板数据/连板滚动回测.md](./连板数据/连板滚动回测.md) | 回测 |
| [连板数据/连板预测校准.json](./连板数据/连板预测校准.json) | 校准参数 |
